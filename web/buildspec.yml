version: 0.2

phases:
  install:
    commands:
      - echo Installing dependencies...
      - npm install
  build:
    commands:
      - echo Building React app...
      - npm run build
  post_build:
    commands:
      - echo Build phase completed. Artifacts will be uploaded.
artifacts:
  files:
    - build/**/*
  discard-paths: yes
